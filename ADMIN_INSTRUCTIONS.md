# 🎛️ Инструкция по использованию Админ-панели

## 🚀 Доступ к админ-панели

Откройте в браузере: `http://localhost:5173/admin`

Или добавьте `/admin` к текущему URL вашего сайта.

---

## 📋 Функционал

### 1️⃣ Управление категориями

**Добавление категории:**
1. Откройте вкладку "КАТЕГОРИИ"
2. Заполните форму:
   - Название категории (обязательно)
   - URL изображения (опционально, по умолчанию `/assets/catalog-1.jpg`)
   - Описание (опционально)
3. Нажмите "ДОБАВИТЬ"

**Редактирование категории:**
1. В списке категорий нажмите "Редактировать"
2. Измените данные в форме
3. Нажмите "ОБНОВИТЬ"

**Удаление категории:**
1. Нажмите "Удалить" у нужной категории
2. Подтвердите действие

⚠️ **ВНИМАНИЕ:** При удалении категории удаляются все её подкатегории и товары!

---

### 2️⃣ Управление подкатегориями

**Добавление подкатегории:**
1. Откройте вкладку "ПОДКАТЕГОРИИ"
2. Выберите категорию из выпадающего списка
3. Введите название подкатегории
4. Укажите URL изображения (опционально)
5. Нажмите "ДОБАВИТЬ"

**Удаление подкатегории:**
1. Найдите нужную подкатегорию в списке
2. Нажмите "Удалить"
3. Подтвердите действие

⚠️ **ВНИМАНИЕ:** При удалении подкатегории удаляются все её товары!

---

### 3️⃣ Управление товарами

**Добавление товара:**
1. Откройте вкладку "ТОВАРЫ"
2. Заполните форму:
   - **Категория** (обязательно) - выберите из списка
   - **Подкатегория** (обязательно) - появится после выбора категории
   - **Название товара** (обязательно)
   - Артикул (опционально)
   - Цена (опционально)
   - URL изображения (опционально)
   - Краткое описание (опционально)
   - Полное описание (опционально)
   - Технические характеристики (опционально)
3. Нажмите "ДОБАВИТЬ ТОВАР"

**После добавления товара:**
- Товар автоматически появится в выбранной подкатегории
- Данные сохраняются в `localStorage`
- В консоли браузера появится код страницы товара (если нужен отдельный файл)

**Удаление товара:**
1. Найдите товар в списке
2. Нажмите "Удалить"
3. Подтвердите действие

---

## 💾 Сохранение и экспорт данных

### Автоматическое сохранение
Все изменения автоматически сохраняются в `localStorage` браузера.

### Экспорт в JSON
1. Нажмите кнопку "ЭКСПОРТ ДАННЫХ (JSON)" в шапке админ-панели
2. Файл `catalog.json` автоматически скачается
3. Замените содержимое файла `src/data/catalog.json` на скачанный

### Импорт данных
Данные загружаются в следующем порядке:
1. Из `localStorage` (если есть)
2. Из файла `src/data/catalog.json` (если localStorage пуст)

Чтобы сбросить к начальному состоянию:
```javascript
localStorage.removeItem('catalogData')
```
И перезагрузите страницу.

---

## 🔗 Структура URL

После настройки каталога, структура URL будет следующей:

```
/catalog                                           → Список всех категорий
/category/{categoryId}                             → Подкатегории категории
/category/{categoryId}/subcategory/{subcategoryId} → Товары подкатегории
/product/{productId}                               → Страница товара
```

**Примеры:**
```
/category/container-fastenings
/category/container-fastenings/subcategory/twistlocks
/product/BE-A1-260
```

---

## 🛠️ Технические детали

### Где хранятся данные?
- **localStorage** - для текущих изменений
- **src/data/catalog.json** - начальное состояние
- **Консоль браузера** - код страниц товаров для ручного создания

### Как связаны категории → подкатегории → товары?

```json
{
  "categories": [
    {
      "id": "container-fastenings",
      "title": "Крепления для контейнеров",
      "subcategories": [
        {
          "id": "twistlocks",
          "title": "Твистлоки",
          "products": [
            {
              "id": "BE-A1-260",
              "title": "Фитинги угловые"
            }
          ]
        }
      ]
    }
  ]
}
```

### Автоматическая генерация ID
- **Категории и подкатегории:** ID генерируется из названия (латиницей через дефис)
- **Товары:** ID берется из артикула, или генерируется timestamp

---

## 🎨 Визуальный стиль

Админ-панель использует тот же дизайн-систему, что и основной сайт:
- Цвета: черный (#000), белый (#fff), серый (#666)
- Шрифты: системные sans-serif
- Компоненты: Header и Footer наследуются из Layout

---

## ⚡ Горячие клавиши и советы

1. **Быстрый доступ к админке:** Добавьте `/admin` в любой момент
2. **Консоль браузера (F12):** Смотрите логи сохранений и коды страниц
3. **Экспорт данных:** Регулярно экспортируйте для бэкапа
4. **Изображения:** Используйте относительные пути `/assets/...` или полные URL

---

## 🐛 Решение проблем

**Данные не сохраняются?**
- Проверьте, включены ли cookies и localStorage в браузере
- Откройте консоль (F12) и проверьте ошибки

**Товары не отображаются?**
- Убедитесь, что выбрана правильная категория и подкатегория
- Проверьте данные в консоли: `localStorage.getItem('catalogData')`

**Нужно сбросить всё?**
```javascript
localStorage.clear()
location.reload()
```

---

## 📞 Дополнительная помощь

Если возникли вопросы - изучите код в `src/pages/Admin/AdminPanel.jsx`

Все функции прокомментированы и разделены на блоки:
- `========== КАТЕГОРИИ ==========`
- `========== ПОДКАТЕГОРИИ ==========`
- `========== ТОВАРЫ ==========`
- `========== СОХРАНЕНИЕ ==========`

