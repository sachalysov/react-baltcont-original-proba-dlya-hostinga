# ⚡ Быстрый старт

## 🚀 Запуск проекта

```bash
# Установка зависимостей (если не установлены)
npm install

# Запуск dev-сервера
npm run dev
```

Проект откроется на `http://localhost:5173`

---

## 🎛️ Доступ к админ-панели

После запуска проекта откройте:

```
http://localhost:5173/admin
```

Или нажмите на иконку 👤 в шапке сайта.

---

## 📦 Что было создано?

### ✅ Новые компоненты:

1. **AdminPanel** (`/src/pages/Admin/AdminPanel.jsx`)
   - Управление категориями
   - Управление подкатегориями
   - Управление товарами
   - Автоматическое сохранение в localStorage
   - Экспорт данных в JSON

2. **CategoryPage** (`/src/pages/CategoryPage/CategoryPage.jsx`)
   - Динамическая страница категорий
   - Отображает подкатегории

3. **SubcategoryPage** (`/src/pages/SubcategoryPage/SubcategoryPage.jsx`)
   - Динамическая страница подкатегорий
   - Отображает список товаров

4. **ProductPage** (`/src/pages/ProductPage/ProductPage.jsx`)
   - Шаблонная страница товара
   - Загружает данные из localStorage по ID товара

### ✅ Обновлены:

1. **App.jsx** - добавлены динамические роуты
2. **Catalog.jsx** - теперь использует данные из JSON/localStorage
3. **Header.jsx** - добавлена ссылка на админ-панель (👤)

### ✅ Создана структура данных:

- **catalog.json** (`/src/data/catalog.json`) - начальная структура каталога

---

## 🔗 Роутинг

| URL | Описание |
|-----|----------|
| `/` | Главная страница |
| `/catalog` | Каталог категорий |
| `/category/:categoryId` | Страница категории с подкатегориями |
| `/category/:categoryId/subcategory/:subcategoryId` | Страница подкатегории с товарами |
| `/product/:productId` | Страница товара |
| `/admin` | Админ-панель |

---

## 📝 Быстрый туториал

### 1. Добавление категории
1. Откройте `/admin`
2. Вкладка "КАТЕГОРИИ"
3. Заполните форму и нажмите "ДОБАВИТЬ"

### 2. Добавление подкатегории
1. Вкладка "ПОДКАТЕГОРИИ"
2. Выберите категорию
3. Введите название и нажмите "ДОБАВИТЬ"

### 3. Добавление товара
1. Вкладка "ТОВАРЫ"
2. Выберите категорию → подкатегорию
3. Заполните данные товара
4. Нажмите "ДОБАВИТЬ ТОВАР"
5. Товар появится в каталоге автоматически

### 4. Просмотр товара
1. Откройте `/catalog`
2. Выберите категорию → подкатегорию → товар
3. Откроется страница с полной информацией

---

## 💾 Экспорт данных

После работы в админ-панели:

1. Нажмите "ЭКСПОРТ ДАННЫХ (JSON)"
2. Сохраните файл
3. Замените `/src/data/catalog.json` на скачанный файл
4. Данные станут постоянными

---

## 🛠️ Особенности

### Автоматическое сохранение
Все изменения сохраняются в `localStorage` браузера моментально.

### Динамические страницы
Вместо создания файлов для каждого товара, используется один шаблон `ProductPage.jsx`, который загружает данные по ID.

### Обратная совместимость
Старые роуты (`/container-fastenings` и т.д.) продолжают работать.

### Наследование стилей
Все страницы используют общий Header, Footer и визуальный стиль проекта.

---

## 🐛 Сброс к начальному состоянию

Если нужно сбросить все изменения:

```javascript
// В консоли браузера (F12)
localStorage.removeItem('catalogData')
location.reload()
```

---

## 📚 Полная документация

Смотрите [ADMIN_INSTRUCTIONS.md](./ADMIN_INSTRUCTIONS.md) для подробных инструкций.

---

## ✨ Готово!

Админ-панель полностью интегрирована в проект. Можно добавлять категории, подкатегории и товары без написания кода.

